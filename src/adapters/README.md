# Memex Core Adapters

This directory is intended to house future integrations and adapters for external LLM orchestrators, platforms, and third-party APIs (e.g. specialized API clients beyond the default OpenRouter/Fugu implementation).

*Currently empty. Built for forward compatibility.*
