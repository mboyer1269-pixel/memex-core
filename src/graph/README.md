# Memex Core Graph

This directory is intended to house the relational graph logic. In the future, the contents of `src/graph.ts` and advanced hybrid RAG logic (Graph + Vector search) will be modularized and migrated into this directory.

*Currently empty. Built for forward compatibility.*
