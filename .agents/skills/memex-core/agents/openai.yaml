interface:
  display_name: "Memex Core"
  short_description: "Repo-specific patterns and workflows for memex-core"
  default_prompt: "Use the memex-core repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true